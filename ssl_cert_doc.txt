link:https://www.baeldung.com/ops/jenkins-enable-https

 sudo yum install openssl
--------------------------------------------------------
openssl req -newkey rsa:2048 -nodes -keyout key.pem -x509 -days 365 -out certificate.pem
Generating an RSA private key
--------------------------------------------------------
 openssl pkcs12 -inkey key.pem -in certificate.pem -export -out certificate.p12
keytool -importkeystore -srckeystore ./certificate.p12 -srcstoretype pkcs12 -destkeystore jenkinsserver.jks -deststoretype JKS
--------------------------------------------------------
 sudo cp jenkinsserver.jks /var/lib/jenkins/
--------------------------------------------------------
sudo chown jenkins:jenkins /var/lib/jenkins/jenkinsserver.jks
--------------------------------------------------------
sudo nano /etc/systemd/system/jenkins.service
--------------------------------------------------------
[Unit]
Description=Jenkins Continuous Integration Server
After=network.target

[Service]
Type=simple
User=jenkins
Group=jenkins
Environment="JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64"
Environment="JENKINS_HTTPS_PORT=8443"
Environment="JENKINS_HTTPS_KEYSTORE=/var/lib/jenkins/jenkinsserver.jks"
Environment="JENKINS_HTTPS_KEYSTORE_PASSWORD=123456"
ExecStart=/usr/bin/java -Djava.awt.headless=true -jar /usr/share/java/jenkins.war --webroot=/var/cache/jenkins/war --httpsPort=${JENKINS_HTTPS_PORT} --httpsK>

[Install]
WantedBy=multi-user.target

--------------------------------------------------------
sudo systemctl daemon-reload
$ sudo systemctl restart jenkins.service 